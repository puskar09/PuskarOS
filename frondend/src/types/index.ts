/**
 * types/
 *
 * Shared TypeScript type definitions and interfaces.
 * All domain types that are used across 2+ features
 * or imported by services must live here.
 *
 * Rules:
 * - No runtime code — types and interfaces only
 * - Feature-specific types live inside their own feature folder
 * - Shared types live here
 *
 * Planned types:
 * mission.ts     — DailyMission, MissionTask, MissionPhase
 * user.ts        — UserProfile, Theme
 * project.ts     — Project, Sprint, Commit
 * learning.ts    — LearningConcept, Quiz, Note
 * fitness.ts     — Workout, WeightEntry, NutritionLog
 * news.ts        — NewsArticle, NewsSource
 */

// export type { DailyMission, MissionTask } from './mission'
// export type { UserProfile } from './user'
// export type { Project, Sprint } from './project'
